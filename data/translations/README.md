# СИСТЕМА ПЕРЕВОДОВ - Полная документация

## Обзор

Система многоязычных переводов для веб-приложения "Exam System". Поддерживает:
- **English** (en) - LTR
- **Русский** (ru) - LTR  
- **עברית** (he) - RTL

## Структура файлов

```
data/translations/
├── README.md                  ← Этот файл
├── TRANSLATIONS_INDEX.md      ← ⭐ ГЛАВНЫЙ ИНДЕКС (читай первым!)
├── SCHEMA.js                  ← Описание структуры JSON
├── CHANGELOG.md               ← История изменений
├── ADD_NEW_LANGUAGE.md        ← Инструкция по добавлению языков
├── en.json                    ← Английские переводы
├── ru.json                    ← Русские переводы
└── he.json                    ← Еврейские переводы
```

## Быстрый старт

### Просмотр всех ключей
**→ Открыть:** `TRANSLATIONS_INDEX.md`

Там пронумерованные все ключи с IDs типа `100.2`, которые легко отслеживать.

### Добавление нового языка
**→ Следовать:** `ADD_NEW_LANGUAGE.md`

8 простых шагов для добавления любого языка.

### Понимание структуры
**→ Прочитать:** `SCHEMA.js`

Описание, как организована структура JSON файлов.

## Как это работает

### 1️⃣ Автоматическое определение языка

Когда студент загружает экзамен:

```python
# app.py - detect_exam_language()
- Анализирует текст экзамена
- Ищет символы иврита (U+0590 - U+05FF)
- Ищет символы русского (U+0400 - U+04FF)
- Возвращает 'he', 'ru' или 'en'
```

API response `/api/exam/<filename>`:
```json
{
  "questions": [...],
  "language": "he",  ← ключ языка!
  "total_questions": 10
}
```

### 2️⃣ Загрузка переводов на клиенте

```javascript
// exam_script.js - loadExamQuestions()
const data = await fetch(`/api/exam/${examName}`);
const language = data.language;  // 'he', 'ru' или 'en'

await i18n.loadLanguage(language);
document.documentElement.lang = language;

// RTL поддержка для иврита
if (language === 'he') {
    document.documentElement.dir = 'rtl';  // CSS: html[dir="rtl"]
}
```

### 3️⃣ Применение переводов

HTML использует `data-i18n` атрибуты:
```html
<button data-i18n="submit_exam">Submit Exam</button>
```

JavaScript заполняет текст:
```javascript
el.textContent = i18n.t('submit_exam');
// → "Submit Exam" / "Отправить экзамен" / "שלח בחינה"
```

### 4️⃣ Специальные случаи: Стрелки

Некоторые элементы имеют смешанный контент (текст + стрелки):

```javascript
// updateTranslations() добавляет стрелки
prevBtn.innerHTML = `← <span>${i18n.t('previous')}</span>`;
//      ↑ Hardcoded стрелка (не переводится)

nextBtn.innerHTML = `<span>${i18n.t('next')}</span> →`;
//                                                     ↑ Hardcoded стрелка
```

Результат:
- EN: `← Previous` | `Next →`
- RU: `← Назад` | `Далее →`
- HE: `← הקודם` | `→ הבא` (RTL CSS обрабатывает расположение)

## Правила работы со словарями

### ✅ МОЖНО ДЕЛАТЬ
1. **Заменять** значение существующего ключа
2. **Добавлять** новые ключи со своим ID номером
3. **Расширять** систему новыми языками

### ❌ НЕЛЬЗЯ ДЕЛАТЬ
1. **Апендить** к существующим ключам (`.append()`)
2. **Экстендить** объекты через `Object.assign()` или спред оператор
3. **Удалять** старые ключи без обновления всех трех файлов одновременно
4. **Забывать** документировать в индексе

### 📝 ПРОЦЕДУРА ЛЮБОГО ИЗМЕНЕНИЯ
1. Открыть `TRANSLATIONS_INDEX.md`
2. Найти нужный ID или создать новый
3. Обновить **все три файла** (en.json, ru.json, he.json) **одновременно**
4. Обновить `TRANSLATIONS_INDEX.md`
5. Проверить что JSON валиден (не забыть запятые!)

## Индексирование ключей

Каждый ключ имеет ID в формате `XXX.Y`:

| Диапазон | Категория | Примеры |
|----------|-----------|---------|
| 1-10 | System | `1.1` = `_meta` |
| 20-50 | Authentication | `20.1` = `login`, `20.7` = `first_name` |
| 100-200 | Exams | `100.2` = `question_number`, `100.5` = `next` |
| 150-160 | Languages | `150.1` = `english`, `150.3` = `hebrew` |

Это позволяет:
- Быстро найти ключ в индексе
- Восстановить испорченные данные
- Добавлять новые категории (200-210, 300-310 и т.д.)

## Примеры использования в коде

### HTML (Templates)
```html
<!-- Простой i18n -->
<label data-i18n="first_name">First Name</label>

<!-- С комментарием ID -->
<!-- KEY: first_name (ID 20.7) -->
<input id="first-name" />
```

### JavaScript
```javascript
// Простой перевод
const text = i18n.t('question_number');

// С комментарием ID
// TRANSLATION TRACKING: ID 100.2 (question_number) + ID 100.3 (of)
${i18n.t('question_number')} ${question.number} ${i18n.t('of')} ${this.questions.length}
```

### Python/Flask
```python
# API endpoint - возвращает язык
if data.language:
    # TRANSLATION TRACKING: AUTO-DETECTION ID 100.2-100.10
    await i18n.loadLanguage(data.language)
```

## Тестирование переводов

### Локально (Developer mode)
1. Открыть DevTools (F12)
2. Console: `localStorage.setItem('language', 'he')`
3. Перезагрузить страницу → должен быть иврит (RTL)

### Функциональное тестирование
- [ ] Экзамен на каждом языке отображается правильно
- [ ] RTL работает для иврита (вопросы справа)
- [ ] Кнопки Previous/Next имеют стрелки
- [ ] Footer "YaM SOFT ©" переводится
- [ ] Нет hardcoded текстов на странице экзамена

## Перед публикацией

1. **Валидация JSON**
   ```bash
   python -m json.tool data/translations/en.json > /dev/null
   python -m json.tool data/translations/ru.json > /dev/null
   python -m json.tool data/translations/he.json > /dev/null
   ```

2. **Проверка индекса**
   - Все ключи из JSON есть в TRANSLATIONS_INDEX.md?
   - Все IDs уникальны?

3. **Тестирование на устройствах**
   - Windows (LTR и RTL)
   - macOS (LTR и RTL)
   - Mobile (LTR и RTL)

## Часто задаваемые вопросы

**Q: Как добавить новый язык?**
A: Смотри `ADD_NEW_LANGUAGE.md` - там 8 шагов.

**Q: JSON выдает ошибку валидации?**
A: Скорее всего забыл запятую между ключами или неправильно закрыл кавычки. Проверь синтаксис.

**Q: Как восстановить испорченный словарь?**
A: Смотри TRANSLATIONS_INDEX.md - там описание каждого ключа и его значение.

**Q: Почему не работает перевод?**
A: Проверь:
1. Есть ли `data-i18n="ключ"` атрибут?
2. Существует ли такой ключ во всех трех JSON файлах?
3. Не забыл ли вызвать `i18n.t('ключ')`?

**Q: Как добавить новый ключ?**
A: 
1. Выбери ID в диапазоне нужной категории
2. Добавь в TRANSLATIONS_INDEX.md
3. Добавь ключ в все три JSON файла (en, ru, he) одновременно
4. Задокументируй в коде с ремаркой ID

## Контакты и поддержка

Если нашел ошибку или хочешь добавить язык:
1. Проверь этот файл и TRANSLATIONS_INDEX.md
2. Следуй процедуре в "Правила работы со словарями"
3. Обнови CHANGELOG.md

---

**Версия:** 1.0
**Последнее обновление:** 2026-02-03
**Поддерживаемые языки:** en, ru, he
**Статус:** Продакшн ✅

