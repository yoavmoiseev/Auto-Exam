# 🚀 Быстрая инструкция - Сборка Offline версии

## ⚡ За 3 шага:

### 1️⃣ Установить PyInstaller
```bash
pip install pyinstaller
```

### 2️⃣ Запустить сборку
```bash
python build_offline.py
```

### 3️⃣ Готово!
```
📦 ExamSystem-Offline/       ← Папка готова
📦 ExamSystem-Offline.zip    ← ZIP архив готов
```

---

## 📋 Что делает build_offline.py:

✅ Проверяет зависимости  
✅ Копирует все нужные файлы (app.py, templates, static, video, **Exams**)  
✅ Создаёт EXE с PyInstaller  
✅ Упаковывает всё в папку  
✅ Создаёт ZIP архив  
✅ Очищает временные файлы  

---

## 📦 Результат:

```
ExamSystem-Offline/
├── ExamSystem.exe          ← ЗАПУСКАТЬ ЭТО!
├── ИНСТРУКЦИЯ.txt         ← Для пользователей
├── Exams/                  ← 14 примеров экзаменов
│   ├── English ITE.txt
│   ├── Hebrew ITE.txt
│   ├── Russian ITE.txt
│   └── ... (и другие)
├── video/                  ← Видео локально
│   ├── en.mp4
│   ├── he.mp4
│   └── ru.mp4
├── data/                   ← База данных
├── templates/              ← HTML
├── static/                 ← CSS/JS
├── teachers/               ← (создастся при запуске)
└── logs/                   ← (создастся при запуске)
```

**Размер:** ~30-50 MB (папка) | ~20-30 MB (ZIP)

---

## 🎯 Распространение:

### Способ 1: ZIP архив
```
ExamSystem-Offline.zip → отправить/скачать → разархивировать → запустить
```

### Способ 2: Прямая копия
```
ExamSystem-Offline/ → скопировать на флешку → запустить с флешки
```

---

## ✨ Особенности:

- ✅ **Один клик** - ExamSystem.exe запускается сразу
- ✅ **GUI окно** - показывает IP, порт, статус
- ✅ **Автоматический браузер** - открывается сам
- ✅ **Безопасное закрытие** - не закрывается случайно
- ✅ **Без интернета** - всё работает локально
- ✅ **LAN сеть** - ученики подключаются по IP

---

## 🖥️ Поддерживаемые системы:

- ✅ Windows 7
- ✅ Windows 8/8.1
- ✅ Windows 10
- ✅ Windows 11

---

## 📝 Примечания:

1. **Exams/** - Папка с 14 примерами экзаменов включена автоматически
2. **video/** - Все 3 видео (en.mp4, he.mp4, ru.mp4) включены
3. **Нет CDN** - все CSS/JS локально
4. **Нет интернета** - работает полностью offline

---

## ⚠️ Важно:

После сборки **НЕ нужно:**
- ❌ Устанавливать Python
- ❌ Устанавливать pip
- ❌ Устанавливать зависимости
- ❌ Настраивать что-либо

Просто:
✅ Скопировать папку  
✅ Запустить ExamSystem.exe  
✅ Нажать "Запустить сервер"  

---

## 🔧 Если что-то пошло не так:

### PyInstaller не найден:
```bash
pip install pyinstaller
```

### Ошибка при сборке:
1. Проверьте, что все файлы на месте
2. Проверьте, что в папке Exams/ есть файлы
3. Проверьте, что в папке video/ есть видео
4. Запустите от имени администратора

### Нужна помощь:
Смотри подробную документацию: `OFFLINE_README.md`

---

**YaM SOFT** © 2026  
Ветка: `offline-standalone`
